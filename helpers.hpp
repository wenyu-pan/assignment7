#ifndef helpers_h
#define helpers_h
#include <string>
#include <vector>
using namespace std;

vector<string> readIntoVector(string,string);

int checkLevel(string);

bool checkManagement(string);

#endif 